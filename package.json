{
  "name": "vash-lavash-api",
  "version": "1.0.0",
  "description": "",
  "main": "ts-built/index.js",
  "scripts": {
    "gcp-build": "npm run build",
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && npx @google-cloud/functions-framework --target=api",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud functions deploy api --entry-point api --allow-unauthenticated --trigger-http --runtime nodejs16"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.14",
    "@typescript-eslint/eslint-plugin": "^5.1.0",
    "@typescript-eslint/parser": "^5.1.0",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@google-cloud/firestore": "^6.0.0",
    "@google-cloud/functions-framework": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
